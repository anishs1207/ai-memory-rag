Date: 2026-07-04

Step 1: Stage all changed and untracked files in the workspace.
Step 2: Commit the staged changes with the message "rag pipeline added".
Step 3: Push the commit to the remote GitHub repository.

## Journey and Troubleshooting

### 1. Pushing Current Code to GitHub
- **Problem**: We need to persist all recent additions (including Naive, Advanced, and Agentic RAG pipelines) by pushing them to GitHub. Git command was not initially found in the default system PATH.
- **Analysis**: We ran a search and checked if Git exists at standard paths. The user provided the path `C:\Users\Anish\AppData\Local\Programs\Git\bin\bash.exe`, confirming Git is installed in the user's local AppData directory.
- **Solution**: We verified that `git.exe` exists at `C:\Users\Anish\AppData\Local\Programs\Git\cmd\git.exe` and used its absolute path to stage, commit, and push the codebase successfully.
