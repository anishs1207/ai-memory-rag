# Blinky - AI Voice Copilot

# Product Description

@1.md - DONE
@2.md - DONE
@3.md - DONE
@4.md - DONE

# Features:

1. AI Copliot Mode:
- Based on concept of cluely (https://cluely.com/) to build cross native desktop app (using electron) which has access to your screen
- make feature to be not detectable in meetings (google meet, zoom, etc)
- User can write normal messages in chat (returns response in markdown format)
- stores the chat in memory (for privacy) or local sqlite db etc used here, also check if voice responses to be saved or not
- AI will respond in text and voice, which can be muted midway
- User can mute voice responses also
- allow user to message via screenshot attached
(solved a painpoint since user has to ask nothing about screen has to modify), also be able to crop direclty requied pary also else if full screen is not required
- allow transparneyc in this to fully be tranprent so user can ocus on other things
- also be able to minimise entire UI into simple logo/ui (to enable user to avoid when user is not using it)
- ui to shrink and expand right now
- ability to save screenshots of the context shared to LLM to be able to view it

2. Blinky Cursor:
- voice can guide you to do your tasks, plus circle or nat on your screen and make your understand concepts (voice works localy pr not), can talk to it (wispflow like), plus has access to the internte
- based on clicky (built by farza) wherein a cursor is there (shape of cursor) which ability for user to change muliple coloures
- cursor should be shiny, blue by default pointer 
- user can ask question to it (speerate UI) for it,with a voice button in which user can speak into it and it will understand it, use reference of the screen and respond to iser's query by a pinter pointing to exact location to resolve the user's query (via use of LLM to return pixel perfect corrdinates) and if multiple steps involved then show a bunch of pixel (once it explain the first stpe), cursor moves natually like a mouse cursor pointer to next part also explaining with voice response for complex tasks
- also allow user to annotate on the sceen, like whiteboard to be able to mark some place or if user is aksing some questin which is not possible but simple curosr poinsiton rather need annotation to be done on the screen, like for teaching tasks etc (whiteboard like annotation)
- have a stop button to also stop in case of cursor one


3. Open Browser:
- can open a browser on it too (wgich ai can brpwpser, without opening it in th e bacoung) and make it fade to just work with you

ADD MORE STUFF AND IMPLEMENT THIS