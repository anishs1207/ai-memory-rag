Attempt to create a complex agentic factory which spawns agent and does software tasks end to end

delegeation (one agent delegaes to another one)
agent builds features. writes tests, and does
test written after implenation (do not catch bugs), sine they do not test impmneations
validation contract (written before code)
(for a complex projects this can be 100s of assertions)
after each milestone of features
(struniy validations and user testing validator)

- shoyld be done indepemley 
- QA engineer (swpamg application via computer Use fills out forms)
checks pages render correcly and functional flows take place holistically and in a way which is better
- most of time is spend for real world validation (insetad of that
- neither validator 
(validation catched bugs)

structed handoff (when worker finishers a feature), fills structued handoff
(to save context of that , what was implmeted ? what was left etc, that is how system self heals)


Codex Security CLI
(to catch bugs in the codebase)

once we have arcuecture, how to run, it (10 agents running in paaralle)
since agents can conflict (so duplication work comes here)

Second brain:
https://github.com/huytieu/COG-second-brain/commits/main/

run features seraily (only 1 wroker running )
oarallosm (read only stuff like code reviwers)

leads to less error rate and correctness compounds her, be abke to see how much of project completed and budget burnt thigh (mission contrlis decidates )


check factory cli:
https://docs.factory.ai/droid-cli/quickstart

creating agent loops
writes tests for code, writes code, tests it, commits it, review it, DONE
model agnoistic
(which model to use for the diff tasks, model routing)