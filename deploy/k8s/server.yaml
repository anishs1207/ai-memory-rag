apiVersion: v1
kind: Service
metadata:
  name: server
  namespace: inqora
spec:
  selector: {app: server}
  ports:
    - {name: http, port: 3001, targetPort: http}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: server
  namespace: inqora
spec:
  replicas: 1
  selector:
    matchLabels: {app: server}
  template:
    metadata:
      labels: {app: server}
    spec:
      serviceAccountName: inqora-server
      containers:
        - name: server
          image: inqora/server:local
          imagePullPolicy: IfNotPresent
          ports:
            - {name: http, containerPort: 3001}
          envFrom:
            - configMapRef: {name: inqora-config}
            - secretRef: {name: inqora-secrets}
          env:
            - name: PORT
              value: "3001"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef: {name: inqora-secrets, key: POSTGRES_PASSWORD}
            - name: DATABASE_URL
              value: postgresql://inqora:$(POSTGRES_PASSWORD)@postgres:5432/inqora
          readinessProbe:
            httpGet: {path: /health, port: http}
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet: {path: /health, port: http}
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests: {cpu: 250m, memory: 512Mi}
            limits: {cpu: "2", memory: 2Gi}
